// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: health_checks.sql

package store

import (
	"context"
)

const getHealthCheck = `-- name: GetHealthCheck :one
SELECT id, resource_id, enabled, method, path, port, interval_seconds, timeout_seconds, retries, start_period_seconds, created_at, updated_at FROM health_checks WHERE resource_id = $1
`

func (q *Queries) GetHealthCheck(ctx context.Context, resourceID int64) (HealthCheck, error) {
	row := q.db.QueryRow(ctx, getHealthCheck, resourceID)
	var i HealthCheck
	err := row.Scan(
		&i.ID,
		&i.ResourceID,
		&i.Enabled,
		&i.Method,
		&i.Path,
		&i.Port,
		&i.IntervalSeconds,
		&i.TimeoutSeconds,
		&i.Retries,
		&i.StartPeriodSeconds,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const upsertHealthCheck = `-- name: UpsertHealthCheck :one

INSERT INTO health_checks (resource_id, enabled, method, path, port, interval_seconds, timeout_seconds, retries, start_period_seconds)
VALUES ($1, $2, $3, $4, $5, $6, $7, $8, $9)
ON CONFLICT (resource_id) DO UPDATE SET
    enabled = EXCLUDED.enabled, method = EXCLUDED.method, path = EXCLUDED.path,
    port = EXCLUDED.port, interval_seconds = EXCLUDED.interval_seconds,
    timeout_seconds = EXCLUDED.timeout_seconds, retries = EXCLUDED.retries,
    start_period_seconds = EXCLUDED.start_period_seconds, updated_at = now()
RETURNING id, resource_id, enabled, method, path, port, interval_seconds, timeout_seconds, retries, start_period_seconds, created_at, updated_at
`

type UpsertHealthCheckParams struct {
	ResourceID         int64
	Enabled            bool
	Method             string
	Path               string
	Port               *int32
	IntervalSeconds    int32
	TimeoutSeconds     int32
	Retries            int32
	StartPeriodSeconds int32
}

// Health checks (§8.8): one row per resource, gate the rolling update.
func (q *Queries) UpsertHealthCheck(ctx context.Context, arg UpsertHealthCheckParams) (HealthCheck, error) {
	row := q.db.QueryRow(ctx, upsertHealthCheck,
		arg.ResourceID,
		arg.Enabled,
		arg.Method,
		arg.Path,
		arg.Port,
		arg.IntervalSeconds,
		arg.TimeoutSeconds,
		arg.Retries,
		arg.StartPeriodSeconds,
	)
	var i HealthCheck
	err := row.Scan(
		&i.ID,
		&i.ResourceID,
		&i.Enabled,
		&i.Method,
		&i.Path,
		&i.Port,
		&i.IntervalSeconds,
		&i.TimeoutSeconds,
		&i.Retries,
		&i.StartPeriodSeconds,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
