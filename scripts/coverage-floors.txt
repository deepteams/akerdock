# Coverage exceptions to the default 90% unit target.
# These packages are deliberately untested test doubles with no production
# decisions of their own. Every handwritten production package uses the
# default floor enforced by check-go-unit-coverage.sh.
internal/dockerruntime/fake	0
internal/hostops/fake	0
