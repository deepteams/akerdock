# generated by AkerDock — revision 1 — DO NOT EDIT
api:
  dashboard: false
  insecure: true
ping: {}

entryPoints:
  web:
    address: ":80"
  websecure:
    address: ":443"
    transport:
      respondingTimeouts:
        # Ingress HTTP/2 control and data requests are long-lived full-duplex
        # streams. Traefik's 60 s default read timeout resets the whole tunnel.
        readTimeout: 0s
    http3: {}

providers:
  file:
    directory: /dynamic
    watch: true

certificatesResolvers:
  http01:
    acme:
      email: "ops@example.test"
      storage: /acme/acme.json
      httpChallenge:
        entryPoint: web

log:
  level: INFO
accessLog: {}
